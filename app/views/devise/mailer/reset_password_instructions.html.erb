<div class="container my-5">
  <div class="card shadow-lg rounded-4 p-5">
    <h2 class="text-center mb-4 text-info">Password Reset Request</h2>

    <p class="text-center mb-4">Hello <strong><%= @resource.email %></strong>!</p>

    <p class="text-center mb-4">Someone has requested a link to change your password. You can do this through the link below:</p>

    <div class="text-center mb-4">
      <%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token), class: "btn btn-primary btn-lg" %>
    </div>

    <p class="text-center text-muted">If you didn't request this, please ignore this email.</p>
    <p class="text-center text-muted">Your password won't change until you access the link above and create a new one.</p>
  </div>
</div>
